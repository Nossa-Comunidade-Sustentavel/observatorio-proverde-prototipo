import { Recycle, TreeDeciduous, Hammer, Sprout, Trees, Droplets, Waves } from "lucide-react";
import type { IconeKey } from "./data";

export const ICONES: Record<IconeKey, typeof Recycle> = {
  recycle: Recycle,
  tree: TreeDeciduous,
  hammer: Hammer,
  sprout: Sprout,
  trees: Trees,
  droplets: Droplets,
  waves: Waves,
};
